<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%=link_to 'Alpha Blog', root_path, class: 'navbar-brand', id: 'logo'%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%=link_to 'Articles', articles_path, class: 'nav-link'%>
        </li>
        <%if logged_in?%>
          <li class="nav-item">
            <%=link_to 'Create new article', new_article_path, class: 'nav-link'%>
          </li>
        <%end%>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li class="nav-item dropdown m-2">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Your user
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%=link_to 'Edit your profile', edit_user_path(current_user), class: 'dropdown-item'%>
              <%=link_to 'View your profile', user_path(current_user), class: 'dropdown-item'%>
            </div>
          </li>
          <li class="nav-item">
            <%= link_to 'Log out', logout_path, method: :delete, class: 'btn btn-danger m-2' %>
          </li>
        <%else%>
           <li class="nav-item">
            <%= link_to 'Log in', login_path, class: 'btn btn-primary m-2' %>
            <%= link_to 'Sign up', signup_path, class: 'btn btn-primary m-2' %>
          </li>
        <%end%>
      </ul>
    </div>
  </div>
</nav>